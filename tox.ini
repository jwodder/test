[tox]
envlist = py3

[testenv]
skip_install = True
deps =
    keyring
    keyrings-alt
    pycryptodomex
    pytest
    pytest-cov
commands =
    pytest -s --cov=foo test_crypto.py

[pytest]
filterwarnings = error
